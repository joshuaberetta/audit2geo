server {
    listen 80;
    server_name audit2geo.imtools.info;

    client_max_body_size 16M;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    location / {
        proxy_pass http://audit2geo:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Increase timeouts for file processing
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # Access and error logs
    access_log /var/log/nginx/audit2geo_access.log;
    error_log /var/log/nginx/audit2geo_error.log;
}

# HTTPS configuration (uncomment when SSL certificate is ready)
# server {
#     listen 443 ssl http2;
#     server_name audit2geo.imtools.info;
#
#     ssl_certificate /etc/letsencrypt/live/audit2geo.imtools.info/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/audit2geo.imtools.info/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     client_max_body_size 16M;
#
#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
#
#     location / {
#         proxy_pass http://audit2geo:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         proxy_connect_timeout 120s;
#         proxy_send_timeout 120s;
#         proxy_read_timeout 120s;
#     }
#
#     access_log /var/log/nginx/audit2geo_access.log;
#     error_log /var/log/nginx/audit2geo_error.log;
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name audit2geo.imtools.info;
#     return 301 https://$server_name$request_uri;
# }
